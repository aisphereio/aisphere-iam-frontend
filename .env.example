# Production should normally keep these values empty so the browser uses the
# current iam.weagent.cc origin and all traffic passes through Envoy Gateway.
NEXT_PUBLIC_IAM_URL=
NEXT_PUBLIC_GATEWAY_LOGIN_URL=
NEXT_PUBLIC_GATEWAY_LOGOUT_URL=
